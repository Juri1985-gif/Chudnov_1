# hw_9

Гитхаб не позволяет загружать большие файлы, поэтому перед запуском
нужно добавить csv файл с данными по пути hw_9/data/realty_data.csv

## Установка зависимостей

```shell
pip install -r ../requirements.txt
```

## Запуск

```shell
streamlit run ./main.py
```